// !!!
// WARNING: This file is autogenerated
// Only modify code within MANUAL() sections
// or your changes may be overwritten later!
// !!!

use crate::consumer::idp_oauth::OAuth;
use serde::{Deserialize, Serialize};

/// ScopeResult:
#[derive(Serialize, Deserialize, Debug, Clone, Default)]
pub struct ScopeResult {
    /// scope: The name of the scope.
    pub scope: String,
    /// description: A human-readable description of the scope, taken from the RBAC Policy.
    pub description: String,
    /// is_grantable: Indicates whether the scope can be granted. Users can only grant scopes if they have the
    /// required permissions.
    pub is_grantable: bool,
}

pub struct IDP {
    pub oauth: OAuth,
}

impl IDP {
    pub fn new(http_client: crate::client::Client) -> Self {
        Self {
            oauth: OAuth::new(http_client.clone()),
        }
    }
}
